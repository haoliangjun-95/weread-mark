# API Gateway URL
# Use a relative URL in local dev so Vite proxies the request and avoids browser CORS.
VITE_WEREAD_API_URL=/api/agent/gateway
VITE_SKILL_VERSION=1.0.3

# ICP 备案信息（留空则不显示）
VITE_ICP_RECORD_NO=
VITE_ICP_RECORD_URL=
